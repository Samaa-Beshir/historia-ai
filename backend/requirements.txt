fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
chromadb==0.5.23
# chroma-hnswlib is deliberately NOT pinned here: chromadb 0.5.23 hard-pins it
# to ==0.7.6, so any other version makes this file unresolvable by pip.
# On a CPU without AVX2 (pre-Haswell Intel), 0.7.6 segfaults when its HNSW
# index resizes past the first insert batch. Downgrade *after* installing, so
# the resolver is never asked to satisfy the contradiction:
#     pip install --no-deps chroma-hnswlib==0.7.3
onnxruntime==1.19.2
google-generativeai==0.8.3
pypdf==5.1.0
numpy==1.26.4

# testing
pytest==8.3.4
pytest-mock==3.14.0
httpx==0.28.1
